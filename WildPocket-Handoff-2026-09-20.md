# 《旷野》离线动物图鉴：交接文档

交接日期：2026-09-20  
产品定位：面向赴坦桑尼亚及东非 Safari 的普通旅行者，在无网络草原环境中识别动物、查看图鉴、记录“见过”的离线移动端图鉴。  
当前主站：https://wild-pocket-east-africa.wanglexin519.chatgpt.site/  
当前网页源码：`work/safari/dist/`  
当前 App 预览：`work/safari/dist/app-preview.html`

---

## 1. 用户完整需求与已经确认的产品决策

### 核心目标

1. 做一个 iPhone 和 Android 都能使用的 Safari 动物图鉴；无网络也必须可用。
2. 产品不再绑定某一个旅行团行程。早期用户提供的坦桑尼亚行程只用于确定地点覆盖；最终要服务所有来非洲 Safari 的用户。
3. 首页默认地点显示“坦桑尼亚 · 全部”，下拉菜单仍保留具体目的地。
4. 优先实现为可安装离线 Web App（PWA）；同时已做 iOS `WKWebView` 包装工程，为 App Store 上架做准备。

### 首页、筛选与列表

1. 全局筛选：地点、日间/夜间。
2. 筛选后只保留该地点、该时段可能出现的动物。
3. 支持按中文名、英文名和特征搜索。
4. 分类要按有助于野外识别的特征，不是严格生物分类。现有分类：全部、有角的、大型动物、猫科、其他哺乳类、鸟类与爬行类。
5. 动物卡要显示相对遇见概率，例如“约 88%”。
6. 列表默认按遇见概率从高到低；“↓ 概率”点击后切换为“↑ 概率”，从低到高。
7. 支持“只看见过”。
8. 每张卡片支持“见过”及取消标记。

### 内容与详情页

1. 卡片使用真实野生动物照片，不能出现卡通图。
2. 动物介绍要详尽、故事化，参考用户提供的刘冰《这些非洲植物的繁衍本能》：从野外观察切入，解释形态、行为、生境和识别意义，避免单纯百科式罗列。
3. 用户要求每种动物增加 2–3 张照片，并支持左右滑动。当前实现是主图加故事配图；只有部分物种有第二张，尚未完成“每种 2–3 张”。
4. 图片主体必须完整。详情图使用 `object-fit: contain`，避免裁掉鸟头或完整身体。
5. 多图详情在图片左右边缘叠加半透明箭头，不在图片下方展示文字提示。
6. 曾做过正文 TTS，用户认为效果差，已移除。不要恢复。
7. 当前只有东非狒狒页面附带一个近缘黄狒狒的警戒叫声样本。页面必须明确是近缘种，不能称作东非狒狒实录。后续补叫声时要逐种核验物种和授权。

### “我的”、本机数据与离线

1. 底部顶层导航只有两个入口：首页、我的。
2. “我的”的主功能是收集册：所有动物头像；已看过点亮；未看过灰阶；显示总体收集进度。
3. 收集册按稀有度分组查看进度：常见、少见、稀有。
4. 稀有度是本图鉴“相对遇见概率”分组，不是 IUCN 或保护等级：常见 >=55%，少见 20–54%，稀有 <20%。
5. “见过”记录保存到本机 `localStorage` 键 `safari-seen`；不做账号、登录或云同步。每次标记立即写入，并尝试使用 `navigator.storage.persist()`。
6. “我的”页展示“已保存在此设备 · N 种记录”。
7. 用户希望全部离线资料默认下载，不要“我的”页中突兀的大下载卡。当前源码已移除大卡；首次联网打开时后台下载 Shell、照片、故事图片和音频。顶部离线按钮仅用于查看状态或补全失败时重试。

### 当前移动端视觉方向

用户最终给了一张小红书首页截图，要求尤其参考顶部和底部：

- 顶部必须保留刘海 / Dynamic Island 安全区。
- 安全区下是一行轻量导航：左离线图标，中间“图鉴 / 坦桑尼亚”，右搜索图标。
- 分类采用横向文字导航和短下划线选中态，不使用网页感很强的筛选胶囊。
- 底部为白色通栏 Tab Bar，两个等宽按钮，图标在上、文字在下；不要圆角悬浮底栏或大块绿色选中卡。
- 视觉配色维持 Safari 的深绿与低饱和草色，不使用参考图的红色。

**最高优先级的未验收项：** 顶部导航最近发生过逐字折行问题。源码已在末尾加入单行、固定 44pt 左右图标区的修复，但交接前没有拿到用户确认截图。下一个 agent 应先检查顶部是否单行、是否与刘海冲突、是否仍有网页布局感。

---

## 2. 当前实现与关键文件

| 文件 | 用途 |
|---|---|
| `work/safari/dist/index.html` | 页面结构、PWA meta、对话框、顶部/底部导航 |
| `work/safari/dist/app.js` | 筛选、排序、见过记录、详情、收集册、离线逻辑 |
| `work/safari/dist/animals.js` | 55 个动物的基础数据、图片、地点与时段 |
| `work/safari/dist/stories.js` | 详情故事、资料链接、故事配图 |
| `work/safari/dist/habitats.js` | 地点定义与默认“坦桑尼亚 · 全部” |
| `work/safari/dist/style.css` | 响应式界面；末尾是最新移动端覆盖样式 |
| `work/safari/dist/sw.js` | Service Worker 和全量离线缓存清单；当前缓存名 `wild-pocket-v30` |
| `work/safari/dist/app-preview.html` | Codex 内 iPhone 393 × 852 预览壳，含 Dynamic Island 模拟层 |
| `work/safari/dist/assets/` | 真实照片、故事图片、音频 |
| `work/safari/.openai/hosting.json` | Sites Hosting 项目配置，部署时不要删除 |
| `outputs/WildPocket-iOS/` | iOS `WKWebView` 源码工程 |
| `outputs/WildPocket-iOS/WildPocket/Resources/Web/` | iOS App 里打包的静态站点副本 |

### 数据规模与关键变量

- 当前图鉴是 **55 个动物**。用户曾同意继续补到约 100 种，这项尚未完成。
- 当前稀有度数量：常见 8、少见 39、稀有 8。
- `safari-seen`：已见动物 ID 数组。
- `safari-filters`：地点与时段。
- `encounterBase`、`nightLift`：相对遇见概率估计，位于 `app.js`。
- `RARITY_GROUPS`、`rarityOf()`：收集册分组，位于 `app.js`。
- 改动离线资源或主要前端文件后，应递增 `sw.js` 中的 `CACHE` 名称。

---

## 3. 当前状态与待继续项

### 已完成

- 可离线安装的 PWA 基础能力。
- 地点、日夜、搜索、特征分类、概率排序、仅看见过。
- 真实照片卡片、概率、见过交互。
- 详情故事、来源链接、相邻物种对比、部分多图图集。
- 本机收集记录、总体进度、稀有度进度。
- 自动离线下载逻辑。
- iPhone 393 × 852 Codex 预览壳。
- iOS `WKWebView` 源码壳与早期压缩包。

### 后续优先级

1. **视觉 QA：顶部导航。** 用新端口打开预览，确认“图鉴 / 坦桑尼亚”不换行，刘海、安全区、顶部导航和内容之间距离正确。
2. **视觉 QA：自动离线下载。** 首次启动需从“离线 / 准备中”到“已离线”；下载失败时对话框不能阻塞阅读。
3. **补齐图集。** 每种动物应有 2–3 张图；当前不满足。
4. **物种扩充。** 目标约 100 种；当前 55 种。需要继续调研、写数据、找有许可的真实照片并补全离线清单。
5. **同步 iOS 工程。** `outputs/WildPocket-iOS/WildPocket/Resources/Web/` 是早期静态副本，没有自动同步当前 `work/safari/dist/`。要继续 iOS 交付时，先完整同步最新 dist。
6. **iOS 构建。** 当前没有生成签名 IPA 或上架。机器历史上没有完整 Xcode；macOS 15.6 可使用 Xcode 26，但用户因安装包大尚未安装。
7. **清理 CSS。** `style.css` 有多段快速迭代的 mobile override，目前靠文件末尾覆盖。视觉确认后建议合并为一段清晰的手机样式，避免回归。
8. **提交与部署。** 最新移动端改动尚未提交，也未部署；线上站仍是较早版本，不能误称已经发布。

---

## 4. 本地运行与验收

### 启动本地预览

```bash
cd /Users/wanglexin/Documents/Codex/2026-09-17/wo-xi/work/safari
python3 -m http.server 4187 --directory dist
```

打开：`http://127.0.0.1:4187/app-preview.html`

因 Service Worker 使用 `ignoreSearch: true`，旧端口可能提供旧缓存。调试最新版时建议换一个端口。

### 静态检查

```bash
cd /Users/wanglexin/Documents/Codex/2026-09-17/wo-xi/work/safari
node --check dist/app.js
git diff --check
```

### 人工验收路径

1. 首页：刘海安全区、顶部导航一行、地点、日夜、搜索、分类、概率升降排序。
2. 详情：打开有多图的动物，确认图片主体完整、箭头叠在图片里、横滑可切换。
3. 收集：点“见过”，到“我的”检查头像点亮、总进度和稀有度进度变化；重开页面确认数据保留。
4. 离线：首次联网后等待“已离线”；断网刷新后检查首页、照片、故事和音频。
5. 窄屏：检查底部 Tab Bar 不遮住最后一张卡。

---

## 5. 部署与 App Store 说明

### Sites Hosting

- 线上站使用 Sites Hosting，配置在 `work/safari/.openai/hosting.json`。
- 最近确认部署的提交为：`37278ef Replace silent baboon sample with alarm call`。
- 之后的移动端改动都在工作树中，交接时未部署。
- 发布前先视觉验收，再提交全部 `dist/` 改动；按 Sites Hosting skill 的打包和发布流程操作。

### iOS App

- iOS 壳用 `WKWebView` 打开打包在 App 内的静态文件，因此离线能力不依赖在线网页。
- App Store 版本需要：同步 `Resources/Web`、用兼容 Xcode 打开 `WildPocket.xcodeproj`、配置 Team/Bundle ID/图标、真机验证、Archive、上传 App Store Connect。
- 本交接包不含证书、Profile、有效签名或 IPA。

---

## 6. 内容和许可原则

1. 图片使用有明确许可的真实照片，主要来自 Wikimedia Commons；保留 `credit` 信息和详情来源链接。
2. 新增图片需逐张核对物种、作者、许可证与商业使用条件；不要从无授权社交网站下载。
3. 详情故事优先采用保护机构、研究机构和论文；外链仅用于延伸阅读，正文需要离线可读。
4. 不把“相对遇见概率”说成实时预报、准确观测统计或保护等级。

---

## 7. 交接包内容

本包包含：

- 本文档 `WildPocket-Handoff-2026-09-20.md`
- `work/safari/`：当前静态站点源码、离线资源、研究/更新脚本、Hosting 配置
- `outputs/WildPocket-iOS/`：iOS `WKWebView` 源码工程
- `outputs/WildPocket-iOS.zip`：此前生成的 iOS 源码压缩包
- `outputs/旷野动物图鉴-网站文件.zip`：此前生成的网站压缩包

为满足“所有产出”交接，这两个历史压缩包也已嵌套进总包。

继续开发时，应以本包 `work/safari/dist/` 为唯一的最新网页来源，不要以 iOS 的 `Resources/Web` 副本作为最新版本。
